# PUBLIC values only — safe to commit. The publishable/anon key is protected by
# Postgres RLS; it is designed to ship in the browser bundle.
# Secret values (SUPABASE_SERVICE_ROLE_KEY, CRON_SECRET, WEATHERAPI_KEY) are NOT
# here — set them in the Vercel dashboard (Project Settings > Environment Variables).
NEXT_PUBLIC_SUPABASE_URL=https://cpkryzaxvozcbdecuhfc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_90PE-Z5-YJm5eiVUX-Bs0g_lpNABMlJ
